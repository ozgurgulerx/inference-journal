# 02 - Model Mechanics and Bottlenecks

## Scope

- Understand prefill vs decode behavior.
- Explain KV-cache growth, reuse, and long-context effects.
- Connect request shape changes to latency and throughput shifts.

## Notes

Use `notes/` for transformer mechanics, cache lifecycle notes, and bottleneck explanations.

## Experiments

Use `experiments/` for prompt-length sweeps, output-length sweeps, and cache reuse benchmarks.

## Exit Criteria

- Every major latency shift can be explained from first principles.
- Cache behavior is measured, not guessed.
