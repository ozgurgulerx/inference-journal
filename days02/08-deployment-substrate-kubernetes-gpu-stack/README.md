# 08 - Deployment Substrate: Containers and Kubernetes GPU Stack

## Scope

- Package and deploy inference systems on GPU-capable containers and Kubernetes.
- Treat the NVIDIA Container Toolkit, GPU Operator, device plugins, and scheduling as first-class inference infrastructure.

## Notes

Use `notes/` for deployment patterns, container setup, GPU operator behavior, node labeling, and cluster-level design notes.

## Experiments

Use `experiments/` for Docker runs, Kubernetes deployments, GPU scheduling checks, and health-validation steps.

## Exit Criteria

- A GPU-backed serving stack runs in a containerized environment.
- Platform choices are documented with operational evidence.
