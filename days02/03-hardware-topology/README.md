# 03 - Hardware Topology

## Scope

- Build intuition for GPU memory systems, PCIe, NVLink, NVSwitch, and NUMA.
- Understand how topology constrains scaling and scheduling.

## Notes

Use `notes/` for topology maps, placement rules, and hardware bottleneck explanations.

## Experiments

Use `experiments/` for topology-aware benchmarks and placement comparisons.

## Exit Criteria

- You can identify when the topology is the bottleneck.
- Placement choices are documented with evidence.
